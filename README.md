# Acord-PDF-Form-to-convert-JSON-Extractor
Acord PDF Form to JSON Extractor is a Django-based application that extracts text and structured data from ACORD PDF insurance forms and converts them into clean JSON output. It supports OCR for scanned files, automatic field mapping, and REST API integration.
